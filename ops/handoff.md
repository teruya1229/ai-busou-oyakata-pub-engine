# handoff

## 次にやるべき1手
- 次の候補は2択に固定する。
- 1) 4コマ描画プロンプトの文面（表情・構図・背景指定）を最小差分で微調整する
- 2) ここで4コマ描画プロンプト導線をMVPとして固定し、次テーマへ移る

## 判断基準
- すぐ次テーマへ進みたいなら、現状で一区切りにする
- 実運用でコマ別指示の具体性が不足するなら、`js/engine.js` の prompt文面だけ最小追補する
- どちらを選ぶ場合も、既存MVP本体と既存UI導線を壊さないことを優先する

## 今回やらないこと
- 永続化（保存/履歴）
- 外部AI API接続
- Kindle用出力
- 複数画面化や高度なUI演出

## 実装方針
- ルールベースを維持し、Kindle責務は `js/kindle-engine.js` に寄せて拡張する
- 既存MVP本体（`engine.js` / `templates.js`）は触らず、影響範囲を限定する
- UI変更が必要な場合でも、既存導線の追加方式で最小差分にとどめる

## 注意点
- 既存MVP本体（`engine.js` / `templates.js` / 既存3出力導線）は触らない
- Kindle責務は `js/kindle-engine.js` に寄せる
- UI変更は必要最小限を維持し、既存の `chapter-episodes` 再利用方針を崩さない
- 大規模リファクタリングは行わない
- 追加調整する場合も、4コマ描画プロンプト周辺（`buildComicPanelPrompts(...)`）の責務に限定する

## 次の拡張候補
- 現場ジャンル別テンプレ（内装/設備/外構など）
- 文字数制約を考慮したX投稿最適化
- 出力プリセット（朝礼向け/振り返り向け）
- 将来のAPI差し替えポイントのインターフェース化
